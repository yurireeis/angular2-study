var minhaVariavel = 'minha variavel';

function minhaFunc (x, y) {
  return x + y
}

let num = 2
const PI = 3.14

let numeros = [1, 2, 3]

numeros.map(v => v * 2)

class User {
  constructor () {}
  getName (name) { return name; }
}
